# 桌面电子宠物 · 代码结构分析报告

> 分析对象：`cockroach_pet.py`（3720 行） + `宠物控制台.py`（519 行）
> 分析日期：2026-09-09

---

## 一、代码结构总览

### 1.1 文件分工

| 文件 | 行数 | 职责 |
|------|------|------|
| `cockroach_pet.py` | 3720 | 主程序：桌面宠物本体，包含所有游戏逻辑、UI渲染、交互系统 |
| `宠物控制台.py` | 519 | 控制台：启动/停止宠物、切换宠物皮肤、开机自启设置、解锁新宠物 |
| `make_sprites.py` | - | 工具脚本：从 sprite_sheet 生成各皮肤精灵帧（未在本次分析范围内） |

---

### 1.2 cockroach_pet.py 结构概览

```
cockroach_pet.py (3720行)
├── 模块级常量与配置（第1-251行）
│   ├── 透明键色 / FPS / 气泡配置
│   ├── 宠物台词池 PET_LINES（4种宠物 + 回退池）
│   ├── 好感度等级、成就定义
│   ├── RPG系统：稀有度、装备池、副本难度、商店价格
│   ├── 各种CD时长、天气、运势、套装加成
│   ├── 帧定义 FRAMES（walk/idle/scared 三种状态）
│   └── 全局变量 _single_mutex / _PET_INSTANCES
│
├── 模块级工具函数（第253-383行）
│   ├── ensure_single_instance()   单实例互斥体
│   ├── script_dir()               脚本目录
│   ├── load_pet_config()          读取宠物配置
│   ├── pet_display_name()         宠物中文名
│   ├── load_pet_library()         宠物库
│   ├── get_unlocked_pets()        已解锁宠物集合
│   ├── resolve_pet()              未解锁回退
│   ├── enable_dpi_awareness()     DPI感知
│   ├── Rect 类                    矩形数据结构
│   ├── get_virtual_rect()         虚拟桌面范围
│   └── get_work_area()            显示器工作区
│
├── CockroachPet 类（第386-3692行）—— 核心类
│   ├── __init__()                 初始化（约100行状态变量）
│   ├── build_ui()                 UI构建（菜单、精灵、气泡）
│   ├── load_frames()              加载精灵帧
│   ├── 移动系统
│   │   ├── base_speed()           基础速度
│   │   ├── bounds()               当前工作区
│   │   ├── step_along()           沿边缘移动
│   │   ├── check_corner()         转角检测
│   │   ├── on_corner()            转角行为（转弯/环绕/反向）
│   │   ├── move_to_target()       飞向目标点
│   │   ├── snap_to_edge()         吸附到边缘
│   │   ├── wrap_move()            穿墙环绕
│   │   ├── chase_step()           跟随鼠标
│   │   └── move()                 主移动入口
│   ├── 随机行为系统
│   │   ├── schedule_random()      调度随机行为
│   │   ├── random_act()           随机行为执行
│   │   ├── fly_to_random_edge()   随机飞边缘
│   │   └── encounter_event()      奇遇事件
│   ├── 互动系统
│   │   ├── on_click()             左键点击
│   │   ├── on_double_click()      双击
│   │   ├── on_drag_start/move/end() 拖拽
│   │   ├── on_right_click()       右键菜单
│   │   ├── on_hover_enter/leave() 悬停暂停
│   │   ├── feed()                 喂食
│   │   ├── pet_head()             摸摸头
│   │   ├── play_with()            逗它玩
│   │   ├── start_chase()          跟随我
│   │   └── follow_companion()     跟随同伴
│   ├── RPG养成系统
│   │   ├── 经验等级：exp_needed() / add_exp()
│   │   ├── 金币系统：add_gold()
│   │   ├── 装备系统：roll_rarity() / make_gear() / set_bonus() / atk_def()
│   │   ├── 副本冒险：start_adventure() / finish_adventure() / _boss_fight()
│   │   ├── 联手讨伐：coop_boss() / _coop_finish()
│   │   ├── 竞技场：arena_fight()
│   │   ├── 强化：upgrade_gear()
│   │   ├── 分解/合成/重铸：dismantle() / synthesize() / recast()
│   │   ├── 商店：buy_gear()
│   │   ├── 签到：daily_signin()
│   │   ├── 成就：check_achievements()
│   │   ├── 装备图鉴：collect_gear() / show_collection()
│   │   ├── 宠物图鉴：show_pokedex()
│   │   └── 星级系统：add_star_exp() / star_display()
│   ├── 双宠互动系统
│   │   ├── 互搏：pet_battle() / _battle_clash() / _fight_hit() / _battle_resolve() / _battle_tick()
│   │   ├── 合体技：fusion_skill() / _fusion_tick() / _fusion_end()
│   │   ├── 联手讨伐：coop_boss() / _coop_finish()
│   │   └── 跟随模式：follow_companion() / _follow_tick()
│   ├── 技能系统
│   │   ├── 疾跑：use_skill() / _skill_end()
│   │   ├── 必杀技：use_ultimate() / _ult_end()
│   │   └── 专属技能：special_skill() / _special_tick()
│   ├── 小游戏系统（6个）
│   │   ├── start_game()           接住食物
│   │   ├── start_pop_game()       点泡泡
│   │   ├── start_box_game()       猜盒子
│   │   ├── start_whack_game()     打地鼠
│   │   ├── start_dice_game()      猜点数
│   │   └── start_rps_game()       石头剪刀布
│   ├── 可视化/特效系统
│   │   ├── update_glow()          主特效渲染（天气/心情/装备/技能/烟花...）
│   │   ├── _draw_weather()        天气绘制
│   │   ├── _draw_weapon()         武器绘制
│   │   ├── _draw_trinket()        饰品绘制
│   │   └── update_animation()     帧动画更新
│   ├── 气泡系统
│   │   ├── show_bubble()          显示气泡
│   │   ├── hide_bubble()          隐藏气泡
│   │   ├── follow_bubble()        气泡跟随
│   │   └── _ensure_bubble_top()   气泡窗口创建
│   ├── 状态持久化
│   │   ├── load_state()           加载状态
│   │   └── save_state()           保存状态
│   ├── 主循环
│   │   ├── tick()                 主 tick（约 25 FPS）
│   │   ├── apply_geometry()       窗口定位
│   │   └── update_animation()     动画更新
│   └── 菜单操作
│       ├── toggle_pause()         暂停/继续
│       ├── teleport()             随机瞬移
│       ├── rename_pet()           改名
│       ├── set_alpha()            透明度
│       ├── wear_badge()           佩戴勋章
│       ├── unequip()              卸下装备
│       ├── wear_from_inv()        从背包穿戴
│       └── quit()                 退出
│
└── main() 函数（第3695-3720行）
    ├── enable_dpi_awareness()
    ├── ensure_single_instance()
    ├── 创建主宠物
    ├── 创建第二只宠物（如配置）
    └── root.mainloop()
```

---

### 1.3 宠物控制台.py 结构概览

```
宠物控制台.py (519行)
├── 模块级常量与路径（第26-36行）
│   ├── APP_NAME / RUN_KEY         注册表自启项
│   ├── PET_SCRIPT / BAT_SCRIPT    宠物脚本路径
│   ├── LIB_JSON / CFG_JSON / STATE_JSON  配置文件路径
│   └── NO_WINDOW                  无窗口启动标志
│
├── 数据操作函数（第39-139行）
│   ├── load_pet_library()         加载宠物库
│   ├── load_unlocked()            已解锁集合
│   ├── unlock_pet()               解锁新宠物（扣金币）
│   ├── load_pet_config()          读取主宠配置
│   ├── save_pet_config()          保存主宠配置
│   ├── load_second_pet_config()   读取第二宠配置
│   └── save_second_pet_config()   保存第二宠配置
│
├── 进程与自启管理（第142-220行）
│   ├── _ensure_autostart_vbs()    确保VBS启动器存在
│   ├── pythonw_path()             pythonw.exe 路径
│   ├── _ps()                      PowerShell 命令封装
│   ├── pet_pids()                 查询宠物进程PID
│   ├── is_pet_running()           是否在运行
│   ├── start_pet()                启动宠物
│   ├── stop_pet()                 停止宠物
│   ├── get_autostart()            读取自启状态
│   └── set_autostart()            设置自启
│
├── Console 类（第223-509行）—— 控制台UI
│   ├── __init__()                 构建界面
│   ├── fill_pets() / fill_skins() 填充宠物/皮肤下拉
│   ├── on_pet_change() / on_skin_change()
│   ├── do_unlock()                解锁宠物
│   ├── apply_selection()          应用选择（重启宠物）
│   ├── fill_second_pets() / fill_second_skins()
│   ├── on_second_pet_change()
│   ├── refresh_unlock()           刷新解锁按钮状态
│   ├── do()                       执行操作并显示状态
│   ├── refresh()                  刷新界面
│   ├── auto_refresh()             定时刷新
│   └── on_auto_toggle()           自启切换
│
└── main() 函数（第512-519行）
```

---

## 二、核心游戏逻辑 vs UI层代码 区分

### 2.1 核心游戏逻辑（可跨平台复用，约 60% 代码）

以下功能不依赖 tkinter，可以直接移植到 Kivy：

#### 2.1.1 数据与状态模块
- 所有模块级常量（`PET_LINES`, `RARITY_STAT`, `GEAR_POOL`, `DIFFS`, `ACHIEVEMENTS` 等）
- `Rect` 数据类
- `load_state()` / `save_state()` — JSON 状态持久化
- `load_pet_config()` / `pet_display_name()` / `load_pet_library()` / `get_unlocked_pets()` / `resolve_pet()` — 配置读取

#### 2.1.2 移动与行为系统
- `base_speed()` — 基础速度计算
- `step_along()` — 沿边缘移动
- `check_corner()` / `on_corner()` — 转角检测与行为
- `move_to_target()` — 飞向目标点
- `snap_to_edge()` — 吸附到边缘
- `wrap_move()` — 穿墙环绕
- `chase_step()` — 跟随鼠标移动
- `move()` — 移动总调度
- `schedule_random()` / `random_act()` — 随机行为
- `fly_to_random_edge()` — 随机飞边缘

#### 2.1.3 RPG养成系统
- `exp_needed()` / `add_exp()` — 经验等级
- `add_gold()` — 金币管理
- `roll_rarity()` / `make_gear()` — 装备生成
- `set_bonus()` / `atk_def()` — 属性计算
- `start_adventure()` / `finish_adventure()` / `_boss_fight()` / `_boss_reward()` — 副本冒险
- `coop_boss()` / `_coop_finish()` — 联手讨伐
- `arena_fight()` — 竞技场
- `upgrade_gear()` — 装备强化
- `dismantle()` / `synthesize()` / `recast()` — 分解/合成/重铸
- `buy_gear()` — 商店
- `daily_signin()` — 签到
- `check_achievements()` — 成就系统
- `collect_gear()` / `show_collection()` — 装备图鉴
- `add_star_exp()` / `star_display()` — 星级系统
- `_quest_act()` / `_quest_check()` — 每日任务
- `fortune_today()` / `fortune_mult()` — 运势系统
- `add_hunger()` / `add_affinity()` / `add_mood()` — 状态数值管理
- `mood_title()` / `title()` / `affinity_level()` / `display_name()` — 显示文本计算
- `lines()` — 台词系统

#### 2.1.4 双宠互动系统
- `pet_battle()` / `_battle_clash()` / `_fight_hit()` / `_battle_resolve()` / `_battle_tick()` — 互搏系统
- `fusion_skill()` / `_fusion_tick()` / `_fusion_end()` — 合体技
- `follow_companion()` / `_follow_tick()` — 跟随模式
- `_skill_name_for()` — 专属招式名

#### 2.1.5 技能系统
- `use_skill()` / `_skill_end()` — 疾跑技能（逻辑部分）
- `use_ultimate()` / `_ult_end()` — 必杀技（逻辑部分）
- `special_skill()` / `_special_tick()` / `special_skill_name()` — 专属技能

#### 2.1.6 小游戏逻辑（游戏规则部分）
- 接住食物：得分/生命/奖励结算逻辑
- 点泡泡：得分计算逻辑
- 猜盒子：猜中判定逻辑
- 打地鼠：得分计算逻辑
- 猜点数：押注结算逻辑
- 石头剪刀布：胜负判定逻辑

#### 2.1.7 主循环逻辑（tick 内的数值更新）
- 饱腹度衰减、心情衰减、天气变化
- 睡眠模式、午夜特典
- 技能冷却计时
- 自动打架检测
- 周报切换、图鉴成就检查
- 饿晕机制
- 连续点击彩蛋

#### 2.1.8 控制台中的纯逻辑函数
- `load_pet_library()` / `load_unlocked()` / `unlock_pet()`
- `load_pet_config()` / `save_pet_config()`
- `load_second_pet_config()` / `save_second_pet_config()`
- `pet_pids()` / `is_pet_running()` / `start_pet()` / `stop_pet()`（进程管理，跨平台需改写）

---

### 2.2 UI层代码（tkinter 相关，需用 Kivy 重写，约 40% 代码）

#### 2.2.1 窗口与渲染
- `build_ui()` — 整个 UI 构建（`tk.Label`, `tk.Canvas`, `tk.Menu` 等）
- `load_frames()` / `_load_tinted()` — `tk.PhotoImage` 加载
- `apply_geometry()` — 窗口定位（含 `SetWindowPos`）
- `update_animation()` — 帧动画切换（`sprite.configure(image=...)`）
- `update_glow()` — Canvas 特效绘制（全部 Canvas 绘图 API）
- `_draw_weather()` — 天气绘制（Canvas）
- `_draw_weapon()` / `_draw_trinket()` — 装备绘制（Canvas）
- `_draw_heart()` / `_star_points()` — 辅助绘图函数
- `tint_golden()` — 图像染色（PIL，可复用但属于渲染层）

#### 2.2.2 气泡系统
- `show_bubble()` / `hide_bubble()` / `follow_bubble()` — 气泡窗口（`tk.Toplevel`）
- `_ensure_bubble_top()` — 气泡窗口创建

#### 2.2.3 菜单与交互事件
- 所有 `on_click`, `on_double_click`, `on_right_click`, `on_drag_*`, `on_hover_*` — tkinter 事件绑定
- `menu = tk.Menu(...)` 及所有 `menu.add_command()` / `menu.add_cascade()` — 右键菜单
- `refresh_gear_menu()` — 动态菜单重建
- `toggle_pause()` / `teleport()` / `quit()` — 菜单操作（逻辑可复用，触发方式需重写）

#### 2.2.4 独立窗口（Toplevel）
- `show_profile()` — 属性面板详情页（`tk.Toplevel`）
- `show_pokedex()` — 宠物图鉴窗口
- `start_game()` — 接住食物游戏窗口
- `start_box_game()` — 猜盒子游戏窗口
- `start_whack_game()` — 打地鼠游戏窗口
- `start_dice_game()` / `_dice_roll()` — 猜点数游戏窗口
- `start_rps_game()` — 石头剪刀布游戏窗口
- `rename_pet()` — 改名对话框（`tkinter.simpledialog`）

#### 2.2.5 控制台全部 UI
- `Console` 类的全部界面构建代码（`tk.Label`, `tk.Button`, `tk.Frame`, `ttk.Combobox`, `tk.Checkbutton` 等）
- 所有 `*.pack()` / `*.grid()` / `*.configure()` 调用
- `auto_refresh()` 定时刷新

---

## 三、Windows 专属 API 调用清单

### 3.1 cockroach_pet.py 中的 Windows API

| 位置 | API 调用 | 用途 | 跨平台替代方案 |
|------|----------|------|----------------|
| 第28行 | `import ctypes` | 全局引入 | 需条件导入 |
| 第259-265行 | `ctypes.windll.kernel32.CreateMutexW` | 单实例互斥体 | Kivy 可用文件锁或其他IPC机制 |
| 第262行 | `ctypes.windll.kernel32.GetLastError` | 获取错误码 | 同上 |
| 第337行 | `ctypes.windll.shcore.SetProcessDpiAwareness` | DPI 高分辨率感知 | Kivy 内置处理 |
| 第340行 | `ctypes.windll.user32.SetProcessDPIAware` | DPI 感知（备用） | Kivy 内置处理 |
| 第354-359行 | `ctypes.windll.user32.GetSystemMetrics(76/77/78/79)` | 获取虚拟屏幕尺寸 | Kivy `Window.size` 或 `Config` |
| 第365-383行 | `ctypes.windll.user32.MonitorFromPoint` + `GetMonitorInfoW` + `ctypes.wintypes.POINT` + `RECT`/`MONITORINFO` 结构 | 多显示器工作区（避开任务栏） | Kivy 需自行适配 |
| 第3097-3099行 | `ctypes.windll.user32.GetCursorPos` | 获取鼠标位置 | Kivy `Window.mouse_pos` |
| 第3448-3459行 | `ctypes.windll.user32.SetWindowPos` + `argtypes` 声明 | 窗口精确定位（物理像素） | Kivy `Window.top` / `Window.left` |
| 第3332行 | `self._bubble_top.wm_frame()` | 获取顶层窗口句柄（十六进制） | Kivy 对应 API |
| 第3339行 | `ctypes.windll.user32.SetWindowPos` | 气泡窗口定位 | Kivy 窗口定位 |
| 第2732-2773行 | `ctypes.windll.user32.RegisterHotKey` / `UnregisterHotKey` / `GetMessageW` + `_MSG` 结构 + 独立线程消息循环 | 全局热键 Ctrl+F8（切换跟随队形） | 各平台全局热键实现不同 |
| 第524行 | `r.wm_attributes("-transparentcolor", KEY)` | 透明色键（品红穿透） | Kivy `Window.clearcolor` + 透明度 |
| 第523行 | `r.wm_attributes("-topmost", True)` | 窗口置顶 | Kivy `Window.topmost`（部分平台支持） |
| 第522行 | `r.overrideredirect(True)` | 无边框窗口 | Kivy `Window.borderless` |
| 第525-526行 | `r.wm_attributes("-alpha", self.alpha)` | 窗口透明度 | Kivy `Window.opacity` |
| 第3432行 | `os._exit(0)` | 强制退出进程 | 可保留 |
| 第2817行 | `from PIL import Image as _PILImage, ImageTk as _PILTk` | 宠物图鉴立绘缩放（PIL） | PIL 可复用，显示部分需改 |

### 3.2 宠物控制台.py 中的 Windows API

| 位置 | API 调用 | 用途 | 跨平台替代方案 |
|------|----------|------|----------------|
| 第22行 | `import winreg` | 注册表操作 | Linux/macOS 无注册表，用启动项替代 |
| 第201-205行 | `winreg.OpenKey` / `QueryValueEx` | 读取自启状态 | 各平台自启机制不同 |
| 第209-220行 | `winreg.CreateKey` / `SetValueEx` / `DeleteValue` | 设置/取消开机自启 | 各平台自启机制不同 |
| 第35行 | `subprocess.CREATE_NO_WINDOW` | 无窗口启动子进程 | 各平台实现不同 |
| 第142-149行 | VBScript 启动器（`_ensure_autostart_vbs()`，GBK 编码写入） | 开机自启隐藏启动 | 各平台自启机制不同 |
| 第153-157行 | `pythonw.exe` 查找 | 无控制台 Python | Linux/macOS 用 `&` 后台运行 |
| 第160-163行 | PowerShell 命令（`Get-CimInstance Win32_Process` / `Stop-Process`） | 进程查询与终止 | Linux/macOS 用 `ps` / `kill` |
| 第187行 | `subprocess.Popen(..., creationflags=NO_WINDOW)` | 无窗口启动宠物 | 各平台实现不同 |

---

## 四、数据文件依赖清单

### 4.1 JSON 配置文件

| 文件 | 用途 | 读写 | 格式说明 |
|------|------|------|----------|
| `pet_config.json` | 当前选中的宠物/皮肤 + 第二只宠物配置 | 读写 | `{pet, skin, second_pet: {pet, skin}}` |
| `pet_library.json` | 宠物库定义（8种宠物，各含名称、精灵图、解锁价格、皮肤列表） | 只读 | `{pets: {id: {name, sheet, unlock, achieve, skins: [{id, name, hue, sat, val, rainbow?}]}}}` |
| `pet_state.json` | 所有宠物的游戏状态存档（核心数据） | 读写 | `{pet_id: {hunger, affinity, level, ...}}` + 顶层 `_unlocked` 数组 |

### 4.2 图片资源

| 目录/文件 | 用途 | 说明 |
|-----------|------|------|
| `sprites_display/{pet}/{skin}/*.png` | 显示用精灵帧（已缩放/调色） | 每种宠物每皮肤有 3 种状态：walk(4帧) / idle(2帧) / scared(2帧)，每帧有正向和_flip镜像版本，共 16 张 PNG |
| `sprites/{pet}/{skin}/*.png` | 原始精灵帧（图鉴立绘用） | 结构同上，尺寸可能不同 |
| `sprite_sheet_raw.png` | 蟑螂原始精灵表 | `make_sprites.py` 输入源 |
| `sprite_sheet_cat.png` | 猫原始精灵表 | 同上 |
| `sprite_sheet_dog.png` | 狗原始精灵表 | 同上 |
| `sprite_sheet_rabbit.png` | 兔子原始精灵表 | 同上 |
| `sprite_sheet_hamster.png` | 仓鼠原始精灵表 | 同上 |
| `sprite_sheet_corgi.png` | 柯基原始精灵表 | 同上 |
| `sprite_sheet_penguin.png` | 企鹅原始精灵表 | 同上 |
| `sprite_sheet_panda.png` | 熊猫原始精灵表 | 同上 |
| `sprite_preview_all.png` | 精灵预览图 | 开发参考，非运行时依赖 |

### 4.3 其他文件

| 文件 | 用途 | 说明 |
|------|------|------|
| `开机自启.vbs` | 开机自启隐藏启动器 | 运行时动态生成（GBK 编码），控制台功能 |
| `启动电子宠物.bat` | 启动脚本 | 被 VBS 调用，控制台功能 |
| `make_sprites.py` | 精灵帧生成工具 | 开发期工具，非运行时依赖 |

---

## 五、pet_state.json 核心状态结构详解

### 5.1 顶层结构

```json
{
  "rabbit": { ... 兔子的状态 ... },
  "cat": { ... 猫的状态 ... },
  "hamster": { ... 仓鼠的状态 ... },
  "_unlocked": ["cockroach", "cat", "dog", "rabbit", ...]
}
```

- 键为宠物 ID（如 `cockroach`, `cat`, `dog`, `rabbit`, `hamster`, `corgi`, `penguin`, `panda`）
- 值为该宠物的完整状态对象
- 特殊键 `_unlocked`：已解锁的宠物 ID 数组（顶层）

### 5.2 单只宠物状态字段详解

| 字段 | 类型 | 含义 | 默认值 |
|------|------|------|--------|
| `hunger` | int | 饱腹度（0-100），0 饿晕 | 80 |
| `affinity` | int | 好感度（0-100），影响台词亲昵度 | 0 |
| `level` | int | 等级（1+），影响技能解锁和属性 | 1 |
| `exp` | int | 当前经验值，升级公式：`level * 30` | 0 |
| `gold` | int | 当前金币数 | 0 |
| `weapon` | object/null | 已装备武器 | null |
| `armor` | object/null | 已装备防具 | null |
| `trinket` | object/null | 已装备饰品 | null |
| `adv_count` | int | 累计冒险次数 | 0 |
| `total_gold` | int | 累计赚取金币（用于成就判断） | 0 |
| `achieves` | string[] | 已达成成就 ID 列表 | [] |
| `last_signin` | string | 上次签到日期（YYYY-MM-DD） | "" |
| `signin_streak` | int | 连续签到天数 | 0 |
| `quest` | object | 每日任务进度 | `{date, adv, interact, done}` |
| `quest.date` | string | 任务日期（跨天重置） | "" |
| `quest.adv` | int | 今日冒险次数 | 0 |
| `quest.interact` | int | 今日互动次数 | 0 |
| `quest.done` | string[] | 已完成的任务 key | [] |
| `mood` | int | 心情值（0-100），影响行为频率 | 70 |
| `stats` | object | 统计数据（陪伴日志用） | `{feed, pet, play, click, adv, boss, faint}` |
| `stats.feed` | int | 累计喂食次数 | 0 |
| `stats.pet` | int | 累计摸头次数 | 0 |
| `stats.play` | int | 累计逗玩次数 | 0 |
| `stats.click` | int | 累计点击次数 | 0 |
| `stats.adv` | int | 累计冒险次数 | 0 |
| `stats.boss` | int | 累计首领击杀次数 | 0 |
| `stats.faint` | int | 累计饿晕次数 | 0 |
| `uptime_ms` | int | 累计相伴时长（毫秒） | 0 |
| `nickname` | string | 玩家给宠物起的昵称（空则用默认名） | "" |
| `alpha` | float | 窗口透明度（0.55-1.0） | 1.0 |
| `collected` | string[] | 装备图鉴收集的装备名称列表 | [] |
| `fortune` | object | 今日运势 | `{date, val}` |
| `fortune.date` | string | 运势日期（跨天重抽） | "" |
| `fortune.val` | string | 运势值（"大吉"/"吉"/"中"/"凶"） | "中" |
| `essence` | int | 精华数量（分解装备获得，用于合成/重铸） | 0 |
| `badge` | string | 当前佩戴的成就勋章 ID | "" |
| `inventory` | object[] | 背包物品数组（上限 12） | [] |
| `weekly` | object | 本周数据（周报用） | `{week, gold, exp, adv, boss, feed, interact}` |
| `weekly.week` | string | ISO 周号（如 "2026-37"） | "" |
| `weekly.gold` | int | 本周金币获得量 | 0 |
| `weekly.exp` | int | 本周经验获得量 | 0 |
| `weekly.adv` | int | 本周冒险次数 | 0 |
| `weekly.boss` | int | 本周首领击杀次数 | 0 |
| `weekly.feed` | int | 本周喂食次数 | 0 |
| `weekly.interact` | int | 本周互动次数 | 0 |
| `no_feed_streak` | int | 连续不喂食天数（隐藏成就） | 0 |
| `no_feed_egg` | bool | 野外求生彩蛋是否已触发 | false |
| `fusion_count` | int | 合体技发动次数（成就用） | 0 |
| `midnight_day` | string | 午夜特典已触发的日期（一天一次） | "" |
| `win_streak` | int | 当前互搏连胜场数 | 0 |
| `best_streak` | int | 历史最高连胜 | 0 |
| `coop_count` | int | 联手讨伐次数（成就用） | 0 |
| `star_level` | int | 宠物星级（1-5） | 1 |
| `star_exp` | int | 星级经验值 | 0 |

### 5.3 装备对象结构

```json
{
  "name": "小铃铛",        // 装备名称（来自 GEAR_POOL）
  "rarity": "普通",         // 稀有度：普通/优秀/精良/史诗/传说
  "slot": "trinket",       // 槽位：weapon/armor/trinket
  "set": "蛋壳",           // 套装主题（8种之一）
  "upgrade": 0             // 强化等级（0-9）
}
```

### 5.4 稀有度与属性对应

| 稀有度 | 攻击加成 | 防御加成 | 分解精华 | 出售价格 |
|--------|----------|----------|----------|----------|
| 普通 | 1 | 1 | 5 | 20 |
| 优秀 | 2 | 2 | 10 | 40 |
| 精良 | 4 | 3 | 20 | 80 |
| 史诗 | 7 | 5 | 40 | 150 |
| 传说 | 12 | 8 | 80 | 300 |

### 5.5 成就列表（17 项）

| ID | 名称 | 奖励金币 |
|----|------|----------|
| `first_adv` | 首次冒险 | 30 |
| `adv_10` | 冒险达人（累计10次） | 100 |
| `gold_500` | 第一桶金（累计赚500） | 200 |
| `gold_2000` | 小富翁（累计赚2000） | 400 |
| `lv5` | 小有成就（达到5级） | 150 |
| `lv10` | 宠物大师（达到10级） | 300 |
| `gear_epic` | 史诗收藏家 | 100 |
| `gear_legend` | 传说收藏家 | 300 |
| `signin_7` | 签到狂魔（连续7天） | 150 |
| `boss_1` | 屠龙者（首次击败首领） | 300 |
| `mood_90` | 阳光宠物（心情达到90） | 120 |
| `collect_15` | 装备鉴赏家（收集15件） | 200 |
| `survivor` | 野外求生（连续7天不喂食） | 200 |
| `fusion_5` | 双子星（合体技发动5次） | 300 |
| `fighter_3` | 格斗大师（互搏连胜3场） | 300 |
| `coop_10` | 并肩作战（联手讨伐10次） | 300 |
| `pokedex_6` | 宠物收藏家（解锁6种） | 300 |
| `pokedex_8` | 宠物大师（解锁全部8种） | 500 |

---

## 六、宠物库概览

### 6.1 8 种宠物

| ID | 名称 | 解锁价格 | 特殊解锁条件 | 皮肤数 | 专属技能 |
|----|------|----------|--------------|--------|----------|
| `cockroach` | 蟑螂小强 | 免费（初始） | - | 5 | 元气爆发 |
| `cat` | 橘猫 | 免费（初始） | - | 6 | 利爪连击（互搏） |
| `dog` | 柴犬 | 免费（初始） | - | 6 | 忠犬猛扑（互搏） |
| `rabbit` | 小白兔 | 免费（初始） | - | 6 | 飞踢弹跳（互搏） |
| `hamster` | 团子仓鼠 | 600 金币 | - | 5 | 滚球冲击 |
| `penguin` | 摇摇企鹅 | 800 金币 | - | 5 | 冰锋啄击（互搏） |
| `corgi` | 短腿柯基 | 1200 金币 | 成就 collect_15 | 5 | 短腿旋风 |
| `panda` | 滚滚熊猫 | 1500 金币 | 成就 collect_15 | 5 | 泰山压顶（互搏） |

### 6.2 等级技能解锁

| 等级 | 技能 | 效果 |
|------|------|------|
| Lv2 | 疾跑 | 速度翻倍 30 秒，CD 90 秒 |
| Lv4 | 好运 | 副本掉率翻倍 |
| Lv6 | 寻宝雷达 | 副本时间减半 |
| Lv8 | 铁壁 | 防御 +4 |
| Lv10 | 聚宝 + 必杀技 | 金币 +25% / 宠物风暴 |
| Lv12 | 疾风 | 副本时间再减 30% |
| Lv14 | 好胃口 | 饱腹度衰减减半 |

---

## 七、迁移到 Kivy 的关键工作量评估

### 7.1 高难度项

1. **透明置顶无边框窗口** — tkinter 的 `-transparentcolor`（色键穿透）在 Kivy 中需用 `Window.clearcolor` + `BorderlessImage` 或 shader 实现；点击穿透（除蟑螂本体外）需用 `Window.on_touch_down` 过滤 + Win32 API 或平台特定方案
2. **多显示器工作区检测** — `MonitorFromPoint` / `GetMonitorInfo` 是 Win32 专属，Kivy 跨平台需自行封装
3. **全局热键** — `RegisterHotKey` 是 Win32 专属，各平台实现方式不同
4. **窗口精确定位** — `SetWindowPos` 的物理像素定位，Kivy 用 `Window.top` / `Window.left` 需考虑 DPI
5. **开机自启** — 注册表操作是 Windows 专属，需为各平台实现不同方案
6. **右键菜单** — tkinter 的 `tk.Menu` 原生支持，Kivy 需用 `Popup` 或自定义组件
7. **Canvas 2D 特效** — `update_glow()` 中有大量 Canvas 绘图（天气粒子、装备光环、技能特效、烟花等），Kivy 中需用 Canvas 指令重写

### 7.2 中等难度项

1. **精灵帧动画** — `tk.PhotoImage` → `Image` / `Atlas` + `Animation`
2. **气泡窗口** — 独立浮动 Toplevel → Kivy `Popup` 或自定义 `ModalView` / `FloatLayout`
3. **小游戏窗口** — 6 个独立游戏窗口需重写为 Kivy 界面
4. **属性面板 / 宠物图鉴** — Toplevel 窗口 → Kivy 界面
5. **拖拽交互** — tkinter 事件 → Kivy `on_touch_down/move/up`

### 7.3 低难度项（直接复用）

1. 所有纯逻辑代码（移动、RPG、养成、计算）
2. 所有 JSON 数据读写
3. PIL 图像处理（染色、缩放）
4. 随机数、时间、数学计算等标准库功能
5. 宠物台词池、装备池、成就定义等常量数据

---

*报告生成完毕。所有分析基于 cockroach_pet.py（3720行）和 宠物控制台.py（519行）的完整阅读。*
